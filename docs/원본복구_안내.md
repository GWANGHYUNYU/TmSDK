# 3차 촬영 원본 재복사 — ✅ 완료 (2026-09-16)

> ## 결과 요약
>
> 원본 123건이 `calib/th_260914/` 에 복원되어 전부 다시 열었습니다.
>
> | | |
> |---|---|
> | 탈락분 중 대비를 잰 것 | **44건** (8건은 보드를 못 찾음, 2건은 메타 손상) |
> | **탈락분 보드 대비** | **중앙 0.86 ℃** · 범위 0.08 ~ 2.78 |
> | 같은 날 남긴 것 | 중앙 1.22 ℃ · 범위 0.40 ~ 2.79 |
> | 되살린 것 | **5건** → 총 73건 · 328~733 mm |
>
> **두 분포가 크게 겹칩니다.** 대비는 탈락 원인의 *일부*지 전부가 아닙니다
> (아래 3장의 예상과 다른 결과). 자세한 것은 [status.md 0-B](status.md).

**작성** 2026-09-15 · 관련 스크립트 [`scripts/rescue.py`](../scripts/rescue.py)

2026-09-14 촬영분 123건 중 **65건을 「검출 0」으로 판정하고 지웠습니다.**
그런데 지우고 나서야 그 녹화들의 **보드 대비를 잰 적이 없다**는 것이
드러났습니다 — `contrast_c` 는 코너를 찾은 뒤에만 계산되기 때문입니다.

원본을 다시 주시면 ① 대비를 재고 ② 살릴 수 있는 것을 살리겠습니다.

---

## 1. 무엇을 복사하면 되는가

**3차 촬영 원본 폴더 전체**를 주십시오. 지워진 65건만 골라내실 필요 없습니다.
이미 가지고 있는 68건은 **파일명의 시각으로 자동 건너뜁니다.**

```
D:\Coding_2025\TmSDK\incoming\th_260914\      ← 여기에 통째로
    th_260914_before\
    th_260914_350\  _400\  _500\  _600\  _700\  _800\  _1000\
    ...
```

| | |
|---|---|
| **필요한 파일** | `.y16raw` 와 `.y16meta` (짝으로) |
| 있으면 좋은 것 | `.avi` `.csv` — 없어도 됩니다 |
| **폴더 구조** | 원래대로 두십시오. 하위 폴더까지 알아서 훑습니다 |
| 용량 | 약 120 MB |

> ⚠ **`calib/` 에 덮어쓰지 마십시오.** 지금 정리된 68건이 들어 있습니다.
> `incoming/` 처럼 **별도 폴더**에 두시면 됩니다.

---

## 2. 그다음 제가 하는 것

```bash
python3 scripts/rescue.py incoming/th_260914            # 판정만
python3 scripts/rescue.py incoming/th_260914 --apply    # 살릴 것을 calib/ 로
```

녹화 하나에 약 25초, 65건이면 **30분쯤** 걸립니다.

### ① 코너 없이 보드 대비를 잽니다

7×4 가 안 잡혀도 **5×4 · 6×3 · 4×3 같은 작은 격자로 보드 위치만 찾으면**
거기서 검정 칸과 금속 칸의 온도차를 직접 잴 수 있습니다.

7×4 가 잡히는 52건으로 검증했습니다.

| | |
|---|---|
| 7×4 로 잰 대비 vs 작은 격자로 잰 대비 | **상관 r = 0.915** |
| 절대차 | **중앙 0.23 ℃** (상대오차 17 %) |

즉 **±0.2 ℃ 정도의 정확도로 대비를 말할 수 있습니다.**
보드를 아예 못 찾은 녹화는 그렇다고 따로 적습니다.

### ② 훨씬 집요하게 검출합니다

정규화 3종(p1-99 · p1-95 · 이상치클립) × **확대 ×1 · ×2 · ×3** × CLAHE.

**확대가 결정적입니다.** 원래 검출기는 확대를 시도하지 않았는데,
이 사다리를 걸어 보니 지금 가지고 있는 「부분격자」 16건 중
**12건이 7×4 로 승격**했습니다.

> 그래서 지워진 65건 중에도 **상당수가 살아날 가능성이 있습니다.**
> 다만 확실한 것은 원본을 봐야 압니다.

### ③ 판정

| 판정 | 뜻 |
|---|---|
| **살림(전체)** | 7×4 가 잡힘 — 그대로 캘리브레이션에 사용 |
| **살림(부분)** | 6×4 또는 7×3 이 2프레임 이상 — 자세·호모그래피에 사용 가능 |
| **못 살림** | 안 잡힘. **그때의 보드 대비를 ℃ 로 함께 적습니다** |

`--apply` 를 붙이면 살릴 것을 `calib/2026-09-14/thermal/` 로 복사하고,
`d0xxx_20260914_hhmmss` 규칙으로 이름을 붙입니다.

---

## 3. 이걸로 알게 될 것

지금 「3차 검출 실패의 원인은 보드 대비」라고 적어 두었지만, 근거는
**살아남은 68건과 2차 촬영과의 비교**뿐입니다
([status.md 0-B](status.md) 의 경고 참조).

원본이 오면 **탈락한 65건의 대비를 직접 재서** 확정할 수 있습니다.

| 예상했던 것 | **실제로 나온 것** |
|---|---|
| 탈락분 대비가 1 ℃ 아래에 몰려 있다 → 「대비가 원인」 확정 | **아닙니다.** 중앙 0.86 ℃ 이지만 범위가 0.08~2.78 로 넓고, 남긴 것(0.40~2.79)과 크게 겹칩니다 |
| 대비는 2~4 ℃ 인데 안 잡힌다 → 원인은 다른 데 | **일부 그렇습니다.** 탈락분의 11 % 가 대비 2 ℃ 이상인데도 안 잡혔습니다 |

**결론 — 대비는 원인의 일부입니다.** 1 ℃ 미만 비율이 탈락 55 % 대 남김
33 % 로 분명히 갈리지만, 대비만으로는 설명이 안 됩니다. 잘림·가림·흔들림이
함께 작용했습니다.

다만 **세션 간 차이에는 겹침이 없습니다** — 2차 4.06 ℃ 대 3차 1.13 ℃.
그래서 [4차 촬영](confluence/열화상_캘리브레이션_4차_촬영표.md)의
「5~6자세마다 재가열」 지침은 그대로 유효합니다.

### 되살린 것

| 파일 | 격자 | 칸 | 거리 | 대비 |
|---|---|---|---|---|
| `d0328_20260914_184402` | 7x3 | 13.5 px | 328 mm | 0.57 ℃ |
| `d0383_20260914_182751` | 7x3 | 11.6 px | 383 mm | 0.64 ℃ |
| `d0590_20260914_185708` | **7x4** | 7.5 px | 590 mm | 0.57 ℃ |
| `d0600_20260914_183057` | **7x4** | 7.4 px | 600 mm | 0.53 ℃ |
| `d0692_20260914_190417` | **7x4** | 6.4 px | 692 mm | 0.45 ℃ |

**총 73건 · 328 ~ 733 mm.**

---

## 4. 이번 일에서 배운 것

**① 지울 때는 지울 것들의 진단 수치부터 표로 남길 것.**
`inventory.json` 에 일부가 남아 있어 겨우 여기까지 왔습니다.
`consolidate_calib.py` 가 `DELETED.csv` 에 대비·칸 크기·거리를 함께 적도록
고쳤습니다.

**② 검출기를 세게 돌릴수록 기하 검사가 필수입니다.**
확대(×2·×3) + CLAHE 를 낮은 대비 영상에 걸면 **캐노피 잎 무늬에서 가짜
격자가 만들어집니다.** 실제로 1000 mm 계열에서 「칸 20.7 px(=거리 213 mm)」
짜리 검출이 나왔습니다. `rescue.py` 에 세 가지 가드를 넣었습니다.

| 가드 | 기준 |
|---|---|
| 이웃 코너 간격의 최대/최소 | **2.5 미만** (가짜는 4.0·36.4 가 나왔음) |
| 프레임 간 칸 크기 일관성 | 폭/중앙 **0.35 미만** |
| 보드 대비 | **0.35 ℃ 이상** — 깨진 프레임의 모아레에서 7×4 가 잡힌 적 있음 |

이 가드를 넣은 뒤 전날의 승격 12건을 재검증해 **2건을 되돌렸습니다.**
