# 녹화 출력 파일
output/

# 로그 파일
logs/

# 배포 패키지 (용량이 크고 GitHub Releases에서 별도 다운로드)
*.whl
*.deb

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 리소스 (로고 등)
resource/

# 제출용 폴더
submit/

# Claude Code 워크트리
.claude/

# OS
.DS_Store
Thumbs.db

# 캘리브레이션 원본 녹화 (464 MB) — 저장소에 넣지 않는다.
# 다만 무엇이 있는지 적어 둔 목록·설명은 넣는다 (녹화 자체는 아니다).
calib/*
!calib/README.md
!calib/INDEX.csv
!calib/DELETED.csv

# 검출 확인용 덤프 이미지
chk/

# 문서에 붙는 근거 이미지 — 작물·현장 사진이라 업로드하지 않는다.
# 문서 본문(docs/*.md)은 그림 없이도 읽히도록 수치를 본문에 적어 둔다.
docs/figures/

# 중간보고 — 본문은 추적하고, 그림·원자료·프레임은 업로드하지 않는다.
report/mid-term/figures/
report/mid-term/data/
report/mid-term/frames/

# 스크립트가 생성하는 산출물
coverage.png
thermal_calib.json
